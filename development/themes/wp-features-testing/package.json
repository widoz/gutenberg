{
	"name": "wp-features-testing",
	"private": true,
	"description": "The wp-features-testing theme. The scripts build the design system: they generate the SCSS token contract from the JSON one, then compile the stylesheets.",
	"scripts": {
		"tokens": "node design-system/scripts/generate-tokens.mjs",
		"tokens:check": "node design-system/scripts/generate-tokens.mjs --check",
		"build": "./build-styles.sh",
		"start": "node design-system/scripts/watch.mjs"
	}
}
